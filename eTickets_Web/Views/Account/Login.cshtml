@using eTickets_Web.Data.ViewModels;
@model LoginVM;

@{
    ViewData["Title"] = ".: Sisteme Giriş :.";
}

<div class="row">
    <div class="col-md-6 offset-3">
        <p>
            <h4>Hesap Girişi</h4>
        </p>

        @if (TempData["Error"] != null) // bir hata oluşmuşsa
        {
            <div class="col-md-12 alert alert-danger">
                <span><strong>Bir hata oluştu : </strong>@TempData["Error"]</span>
            </div>
        }
        @* Hata yoksa *@

        <div class="row">
            <div class="col-md-8 offset-2">
                <form asp-action="Login">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div class="form-group">
                        <label asp-for="EMailAddress" class="control-label"></label>
                        <input asp-for="EMailAddress" class="form-control" />
                        <span asp-validation-for="EMailAddress" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Password" class="control-label"></label>
                        <input asp-for="Password" class="form-control" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <div class="form-group mt-3">
                        <input class="btn btn-outline-success float-end" type="submit" value="Login"/>
                        <a class="btn btn-outline-secondary" asp-controller="Movies" asp-action="Index"></a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

